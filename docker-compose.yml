services:
  hive-research-gpu:
    build:
      context: .
    network_mode: host
    volumes:
      - hive_data:/app/data
    environment:
      - OLLAMA_BASE_URL=http://localhost:8210
      # Keep embeddings off the chat model's instance: a 27B chat model
      # monopolizing VRAM makes embed requests stall for minutes.
      - OLLAMA_EMBED_BASE_URL=http://localhost:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-qwen3.6:latest}
      - OLLAMA_FAST_MODEL=${OLLAMA_FAST_MODEL:-llama3.2:3b}
      - OLLAMA_EMBED_MODEL=${OLLAMA_EMBED_MODEL:-nomic-embed-text}
      # Long-form generation (surveys, deep answers) on thinking models
      # needs several minutes; 180s aborted them mid-write.
      - OLLAMA_TIMEOUT=900
      # Set HIVE_TOKEN to require auth on every request; the healthcheck
      # below automatically presents it.
      - HIVE_TOKEN=${HIVE_TOKEN:-}
    shm_size: 8gb
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f -s -o /dev/null -w '%{http_code}' -H \"X-Hive-Token: $${HIVE_TOKEN:-}\" http://localhost:7777/api/stats | grep -q 200"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 10s

  companion:
    build:
      context: .
      dockerfile: Dockerfile.companion
    network_mode: host
    depends_on:
      hive-research-gpu:
        condition: service_healthy
    volumes:
      - companion_data:/app/data/companion
    environment:
      # The companion drives the app through its HTTP API (single writer).
      - HIVE_API_URL=http://localhost:7777
      - HIVE_TOKEN=${HIVE_TOKEN:-}
      - OLLAMA_BASE_URL=http://localhost:8210
      - OLLAMA_FAST_MODEL=${OLLAMA_FAST_MODEL:-llama3.2:3b}
      - OLLAMA_IDEATION_URL=http://localhost:11434
    restart: unless-stopped

volumes:
  hive_data:
  companion_data:
